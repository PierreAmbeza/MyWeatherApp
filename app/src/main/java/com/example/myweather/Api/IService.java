package com.example.myweather.Api;

interface IService {
/*
    @GET("posts")
    Call<List<Post>> getPosts();
    @GET("posts/{postId}" )
    Call<Post> getPost(@Path("postId") long postId);
    @GET("comments")
    Call<List<Comment>> getComments (@Query("postId") long postId);
    @POST("posts")
    Call<Post> addPost(@Body Post post);
    @PUT("posts/{postId}" )
    Call<Post> replacePost (@Path("postId") long postId, @Body Post post);
    @PATCH("posts/{postId}" )
    Call<Post> updatePost(@Path("postId") long postId, @Body Post post);
    @DELETE("posts/{postId}" )
    Call<Void> deletePost(@Path("postId") long postId);
*/
}
